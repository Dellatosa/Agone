<div class="carte reconn-oeuvre" data-owner-id="{{owner}}">
    <div class="entete">
        <!-- <img src="{{img}}" title="{{name}}" height="32" width="32"/> -->
        <span>{{localize "agone.actors.reconnOeuvre"}}</span>
    </div>
    <div class="grid-2-chk">
        <label>{{ localize "agone.chat.utiliseHeroisme"}}</label>
        <input class="util-hero" type="checkbox" name="utiliseHeroisme"/>
    </div>
    <div>{{ localize "agone.chat.reconnOeuvreMsg" }}</div>
     <div class="boutons-carte grid-2">
        {{#each arts as | art id | }}
            <button class="jet-reconn-oeuvre" data-art-id="{{id}}">{{art.label}}</button>
        {{/each}}
    </div>
</div>